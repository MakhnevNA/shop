export function dynamicSort(categories, sort) {
    if (categories === "Все" && sort === "По порядку") {
        return { order: 1 };
    }

    // if(categories === 'Все' && sort === 'По порядку')
}
